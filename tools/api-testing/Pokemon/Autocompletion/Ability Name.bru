meta {
  name: Ability Name
  type: http
  seq: 2
}

get {
  url: {{BASE_URL}}/pokemon/autocomplete/ability?query=power
  body: none
  auth: inherit
}

params:query {
  query: power
  ~limit: 15
}

settings {
  encodeUrl: true
  timeout: 0
}
