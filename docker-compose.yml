name: tcg-decklists-new

services:
  database:
    image: postgres:18.0-alpine3.21
    restart: always
    environment:
      POSTGRES_PASSWORD: testing1234
      POSTGRES_DB: tcg_decklists
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql

  # backend:
  #   depends_on:
  #     - database
  #   build:
  #     context: ./backend
  #     dockerfile: Dockerfile
  #   # TODO add other parameters

  # frontend:
  #   depends_on:
  #     - backend
  #   build:
  #     context: ./frontend
  #     dockerfile: Dockerfile
  #   # TODO add other parameters

volumes:
  pgdata:
